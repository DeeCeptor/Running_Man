using UnityEngine;
using System.Collections;

public class SpotlightWander : MonoBehaviour {

    Random random;

	// Use this for initialization
	void Start () {
        random = new Random();
	}
	
	// Update is called once per frame
	void Update () {
	    transform.rotation = new Vector3(
	}
}
